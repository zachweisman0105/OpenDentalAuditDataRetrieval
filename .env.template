# Environment Variables (Fallback Credential Storage)
# 
# NOTE: Using OS keyring is STRONGLY RECOMMENDED for production use.
# Environment variables are less secure and should only be used when keyring is unavailable.
#
# To configure credentials securely, run:
#   opendental-cli config set-credentials
#
# If keyring is unavailable, copy this file to .env and fill in values:

# OpenDental API base URL (required)
# Example: https://example.opendental.com/api/v1
OPENDENTAL_BASE_URL=

# OpenDental API key for Bearer token authentication (required)
# Keep this value SECRET - do not commit to version control
OPENDENTAL_API_KEY=

# Environment name (optional, default: production)
# Options: production, staging, dev
OPENDENTAL_ENVIRONMENT=production

# Mock mode for local development (optional, default: false)
# When enabled, uses local fixtures instead of making API calls
# OPENDENTAL_MOCK_MODE=false
